import { useRouter } from "next/navigation";

export default function Navigation() {
  const router = useRouter();

  return (
<div className="w-full min-h-20 VStack items-center justify-center bg-red-500 text-center"> Footer</div>
);
}
